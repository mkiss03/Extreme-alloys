/** @type {import('next').NextConfig} */
// Updated: Next.js automatically exposes NEXT_PUBLIC_* env vars to the browser
// No need to manually configure them in next.config.mjs
const nextConfig = {
  reactStrictMode: true,
}

export default nextConfig
